services: 
  node-app:                                                
    build:
      context: .
      args:
        - NODE_ENV=development
    volumes: 
      - ./src:/app/src:ro              ### NOTE: here you can use (relative path)
    environment:
      - NODE_ENV=development
    command: npm run start-dev